#include <stdio.h>
/**
 * main - the main function of the program is to print
 *
 * Return: the program should always end in a 0
 */
int main(void)
{
	int a = 0;
	int b = 0;
	int coma = 1;

	while (a <= 9)
	while (b <= b)
	if (coma == 0)
	putchar(',');
	putchar(32);
	coma = 0;
	putchar(a + '0');
	putchar(b + '0');
	++a;
	++a;
	b = 0;
	putchar(10);
	return (0);
}
